import type { Config } from "tailwindcss";

// Tailwind CSS v4 is configured in CSS via @import/@theme directives.
// This stub remains for tooling that expects a config file.
const config: Config = {
  content: [],
};

export default config;
